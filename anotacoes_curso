:: ANOTAÇÕES CURSO DATA SCIENCE ::

:: Command Line

pwd  primary working directory
ls -l : lista detalhes dos arquivos e subdiretorios do diretorio
ls -a : lista arquivos e subdiretorios ocultos
cd .. : move para um diretorio acima
mkdir : make directory
touch : creates a empty file
cp : copy
cp -r diretorio1 diretorio2  : copia recursivamente todo o conteudo de um diretorio para outro
rm : remove um arquivo
rm -r : remove recursivamente um diretorio e todos os arquivos dentrogit
mv file directory : move files between directories
mv file renamed_file : renomeia um arquivo
date : diz data e hora
echo : escreve na tela

:: GIT

git config --global user.name "" : confifugra o usuario global
git config --global user.email "" : configura o email global
git init : initialize a local git repository in this directory
git remote add origin https://github.com/username/repo.git : linka o diretorio com o repositorio no github
git add . : adds all new files to index
git add -u : updates tracking for files that changed names or were deleted
git add -A : does both
git commit -m "message" : commit in the local repo , the message is a useful description of what you did
git push : sobe as alteracoes pro github
git checkout -b branchname : creates a new branch
git branch : to see what branch you are
git checkout master : to switch back to the master branch type
git pull : pra mergear branchs diferentes





